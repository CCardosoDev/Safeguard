<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".ActivityPackagesSelector">

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textViewSettingsPackageSelector"
        android:text="@string/packages_selector_title"
        android:textSize="30dp"
        android:layout_alignParentTop="true" />

    <ListView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/listViewApps"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/textViewSettingsPackageSelector"
        android:layout_above="@+id/buttonSaveAppList"/>

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonSaveAppList"
        android:layout_marginTop="10dp"
        android:onClick="onClickbuttonSaveApps"
        android:text="@string/guardar"
        android:layout_alignParentBottom="true" />

</RelativeLayout>
